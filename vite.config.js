import { defineConfig } from "vite";

// If your repo is NOT <user>.github.io, GitHub Pages serves under /<repo>/
// Replace "cookers" with your repo name or set base: "" for root.
export default defineConfig({
  base: "/cookers/"
});
